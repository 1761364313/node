"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const dbConnect_1 = require("../utils/dbConnect");
const setTrans_1 = __importDefault(require("../utils/setTrans"));
const constants_1 = require("../constants/constants");
const error_1 = require("../constants/error");
function fetchSet(structure, params = {}, query = {}) {
    return new Promise((resolve, reject) => {
        let tempSet = setTrans_1.default(params, Object.assign({ structure }, query), constants_1.PLATFORM_NAME.REDIS);
        if (tempSet.keyValue.length < 2) {
            throw new Error(error_1.REDIS_SET_ERROR);
        }
        dbConnect_1.redisClient[tempSet.method](...tempSet.keyValue, (err, reply) => __awaiter(this, void 0, void 0, function* () {
            try {
                if (err) {
                    reject({ err });
                }
                if (query.expireSec) {
                    for (let k = 0; k < tempSet.key.length; k++) {
                        yield dbConnect_1.redisExpire(tempSet.key[k], query.expireSec, 1);
                    }
                }
                else if (query.expireMillisec) {
                    for (let k = 0; k < tempSet.key.length; k++) {
                        yield dbConnect_1.redisExpire(tempSet.key[k], query.expireMillisec, 2);
                    }
                }
                else if (query.expireAt) {
                    for (let k = 0; k < tempSet.key.length; k++) {
                        yield dbConnect_1.redisExpire(tempSet.key[k], query.expireAt, 3);
                    }
                }
                else if (query.expireMilliAt) {
                    for (let k = 0; k < tempSet.key.length; k++) {
                        yield dbConnect_1.redisExpire(tempSet.key[k], query.expireMilliAt, 4);
                    }
                }
                resolve({ data: reply });
            }
            catch (err2) {
                reject(err2);
            }
        }));
    });
}
exports.default = fetchSet;
